$ORIGIN ikm.internal.
$TTL 345600

@	IN	SOA	ns1.ikm.internal. admin.ikm.internal. (
		2025031623
		28800
		7200
		864000
		345600
		)

	IN	NS	ns1.ikm.internal.

ns1	IN	A	{{ hostvars['knotdns']['interface_ip'] | default(hostvars['knotdns']['ansible_host'], true) }}

www	IN	HTTPS	10	. (
		ipv4hint={{ hostvars['nginx']['interface_ip'] | default(hostvars['nginx']['ansible_host'], true) }}
		alpn=h3
		)
