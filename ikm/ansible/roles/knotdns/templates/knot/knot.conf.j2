server:
  rundir: /run/knot
  user: knot:knot
  automatic-acl: on
  listen: {{ hostvars['knotdns']['ansible_host'] }}@53

log:
  - target: syslog
    any: info

database:
  storage: /var/lib/knot

template:
  - id: default
    storage: /var/lib/knot

zone:
  - domain: ikm.internal
    file: internal.ikm.zone
