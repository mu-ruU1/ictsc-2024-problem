all:
  hosts:
    knotdns:
      ansible_host:
    knotres:
      ansible_host:
    nginx:
      ansible_host:
    client:
      ansible_host:
  vars:
    ansible_user:
    ansible_password:
    ansible_become_password:
    ansible_ssh_private_key_file:
